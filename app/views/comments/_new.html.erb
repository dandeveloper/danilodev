<%= form_for([@post, Comment.new], remote: true) do |f| %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, size: '50x20' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
